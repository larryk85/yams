set(PACKAGE_VERSION @yams_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@yams_VERSION_MAJOR@")
   if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@yams_VERSION_MINOR@")
      set(PACKAGE_VERSION_MATCH TRUE)
   elseif("${PACKAGE_FIND_VERSION_MINOR}" LESS "@yams_VERSION_MINOR@")
      set(PACKAGE_VERSION_COMPATIBLE TRUE)
   else()
      set(PACKAGE_VERSION_UNSUITABLE TRUE)
   endif()
else()
   set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
